tags
test.sh
.luarc.json
nvim

spell/

# Tracked in this fork, as upstream recommends - see :help vim.pack-lockfile.
# Upstream ignores it only to avoid merge conflicts in the shared template repo.
# nvim-pack-lock.json

.DS_Store
